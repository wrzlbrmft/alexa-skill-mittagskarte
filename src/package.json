{
  "name": "alexa-skill-mittagskarte",
  "version": "0.0.0",
  "dependencies": {
    "alexa-sdk": "*",
    "cheerio": "*",
    "request": "*",
    "@types/alexa-sdk": "*",
    "@types/cheerio": "*",
    "@types/node": "*",
    "@types/request": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "typings": "*"
  },
  "scripts": {
    "postinstall": "typings install && tsc",
    "build": "tsc",
    "clean": "rm -rf build node_modules typings"
  }
}
